```mermaid
classDiagram 
    class Cell {
        -charge: float
        +Name: string
        +Charge: float
        +Level: int
        +Consume() void
        +Restore() void
        +ToString() string
        
    }
```